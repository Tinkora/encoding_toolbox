{
  "name": "encoding-toolbox-web-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "prepare:wasm": "wasm-pack build --target web --out-dir static/pkg",
    "serve": "http-server . -a 127.0.0.1 -p 4197 -c-1 --silent",
    "test": "npm run prepare:wasm && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "http-server": "14.1.1"
  }
}
